@{
	Layout = "_CleanLayout";
	ViewData["Title"] = "Login";
}

<div class="center-screen">
	<div class="surface">
		Form to log in
		<button class="btn btn-blue">@ViewData["Title"]</button>
	</div>
</div>